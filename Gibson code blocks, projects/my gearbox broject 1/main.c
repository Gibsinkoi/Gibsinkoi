#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c;
    char op;
    printf("%\n",Enter number)
    printf("Enter first number\n");
    scanf("%d", &a);
    printf("Enter second number\n");
    scanf("%d", &b);

_
}
    if (op=='+'){
        c=a+b;
        printf("the sum is %d,",c);
        }else if (op=='-'){
         c=a-b;
        printf("the difference is %d ",c);
        }
        else if (op=='x'){
            c=a*b;
            printf("the product is %d",c);
            }
        else if (op=='*'){
        c=a*b;
        printf("the share %d,",c);
        }
        }
            else {
                printf("invalid choice");
            }

            return 0;

}
